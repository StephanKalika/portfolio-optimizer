fastapi
uvicorn[standard]
pydantic
torch==2.0.1
SQLAlchemy==1.4.27  # Or a more recent 1.x version, ensure compatibility
psycopg2-binary==2.9.1 # For PostgreSQL connection
pandas==1.3.5 # Added for data manipulation
numpy==1.24.3 # Updated to newer version compatible with PyTorch 2.0.1
scikit-learn==1.0.2 # For MinMaxScaler and train_test_split
matplotlib==3.5.1 # For visualization if needed
joblib==1.1.0 # For saving models and scalers
# torchvision and torchaudio are often installed with PyTorch, but we might not need them explicitly yet.
# We can add them if specific functionalities require them later. 